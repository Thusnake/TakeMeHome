class Player:
    def __init__(self):
        self.health = 100
        self.money = 1000

    increaseHealth(amount):
     self.health += amount
    decreaseHealth(amount):
     self.health -= amount
    increaseMoney(amount):
     self.money += amount
    decreaseMoney(amount):
     self.money -= amount
    getHealth():
     return self.health
    getMoney()
     return self.money
